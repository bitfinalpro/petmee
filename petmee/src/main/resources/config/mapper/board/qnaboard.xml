<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.petmee.repository.dao.QnaBoardDAO">

<!-- 리스트 , 페이징 -->
	<select id="selectQnaBoard" resultType="QnaBoard" parameterType="Page">
	   select *
	     from bitfinalproject.tb_qna
	    order by qna_no desc
	     limit #{begin}, 10
	</select>
	<select id="selectQnaCount" resultType="int">
	select count(*)
		from bitfinalproject.tb_qna
	</select>
<!-- 글 상세 조회 -->
	<select id="selectOneBoard" parameterType="int" resultType="QnaBoard">
		select *
		  from bitfinalproject.tb_qna
		 where qna_no = #{qnaNo} 
	</select>
<!-- 게시글 등록 -->
	<insert id="insertQnaBoard" parameterType="QnaBoard">
		insert into bitfinalproject.tb_qna (
			 qna_title, qna_writer, qna_content
		) values (
			 #{qnaTitle}, #{qnaWriter}, #{qnaContent}
		)
	</insert>
<!--  조회 수 -->	
	<update id="updateViewCnt" parameterType="int">
		update bitfinalproject.tb_qna 
		   set view_cnt = view_cnt + 1
		 where qna_no = #{qnaNo}
	</update>
<!--  글 수정 -->	
	<update id="updateQnaBoard" parameterType="QnaBoard">
		update bitfinalproject.tb_qna
		   set qna_title = #{qnaTitle},
		       qna_writer = #{qnaWriter}, 
		       qna_content = #{qnaContent}
		 where qna_no = #{qnaNo}
	</update>
<!--  글삭제 -->
	<delete id="deleteQnaBoard" parameterType="int">
		delete 
		  from bitfinalproject.tb_qna 
		 where qna_no = #{qnaNo}
	</delete>
<!-- 자원봉사 후기 검색 게시물 추출  -->
	<select id="selectSearchWriter" resultType="QnaBoard" parameterType="Search">
	   select *
	     from bitfinalproject.tb_qna
	    where writer like concat('%',#{searchText},'%')
	    order by no desc
	     limit #{begin}, 10
	</select>	
	<select id="selectSearchTitle" resultType="QnaBoard" parameterType="Search">
	   select *
	     from bitfinalproject.tb_qna 
	    where title like concat('%',#{searchText},'%')
	    order by no desc
	     limit #{begin}, 10
	</select>	
	<select id="selectSearchBoth" resultType="QnaBoard" parameterType="Search">
	   select *
	     from bitfinalproject.tb_qna
	    where (title like concat('%',#{searchText},'%')) OR (writer like concat('%',#{searchText},'%'))
	    order by no desc
	     limit #{begin}, 10
	</select>		
	
</mapper>






















